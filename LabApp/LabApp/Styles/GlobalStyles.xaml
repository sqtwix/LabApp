<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- Стиль для карточки. Используем DynamicResource для цветов, определённых в BundledTheme -->
    <Style x:Key="LoginCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource MaterialDesignCardBackground}" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.3" />
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="30" />
    </Style>

    <!-- Стиль для кнопки -->
    <Style x:Key="LoginButtonStyle" TargetType="Button"
           BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Primary.Mid}" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Primary.MidForeground}" />
        <Setter Property="Width" Value="120" />
        <Setter Property="Height" Value="40" />
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Margin" Value="0,20,0,0" />
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="10" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.3" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стили для текстовых полей -->
    <Style x:Key="TextBoxStyle" TargetType="TextBox"
        BasedOn="{StaticResource MaterialDesignComboBoxEditableTextBox}">
        <Setter Property="Width" Value="120" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Margin" Value="0,20,0,0" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="8" />
    </Style>
</ResourceDictionary>